# This is an optional script that will be appended to the `runfile.sh` script.
# The script will be appended before sourcing the ROS2 workspace setup file, before the ros2 launch command.

# Do something here before sourcing the ROS2 workspace setup file.
# echo "[source_env]"
