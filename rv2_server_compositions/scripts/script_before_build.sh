# This is an optional script that will be executed before the colcon build process.

#!/bin/bash

# echo "[script_before_build]"

# The argument $1 should be the path to the package under ROS2 workspace source directory.
# echo ${@}
